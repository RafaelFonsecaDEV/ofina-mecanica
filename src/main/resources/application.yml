spring:
  application:
    name: oficina mecanica
  datasource:
    url: jdbc:h2:mem:oficina mecanica
    username: sa
    password: 12345
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled:
        path: /h2-console //pode alterar para qualquer nome
